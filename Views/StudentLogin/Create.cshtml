@model MVCCRUD.tblStudent

@{
    ViewBag.Title = "StuLogin";
}

<h2>StuLogin</h2>




<div class="form-horizontal">
    @using (Html.BeginForm("Create", "StudentLogin", FormMethod.Post, new { id = "abc" }))
    {
        @Html.AntiForgeryToken()

        <div class="form-group">
            <input type="text" id="txtphone" name="Name" class="form-control" placeholder="mobile number" />
        </div>

        <div class="form-group">
            <input type="password" id="txtpassword" name="Course" class="forn-control" placeholder="password" />
        </div>

        <div>
            <button class="btn btn-sm btn-success">Sign-In</button>
        </div>

        @*<div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Create" class="btn btn-default" />
                </div>
            </div>*@
  
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
